import React from "react";
import PropTypes from "prop-types";

const Home = props => {
  return <input type="button" className={"button input type"} value={2} />;
};

Home.propTypes = {};

export default Home;
