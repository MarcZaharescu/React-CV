import React from "react";
import PropTypes from "prop-types";

const Projects = props => {
  return <input type="button" className={"button input type"} value={5} />;
};

Projects.propTypes = {};

export default Projects;
