import React from "react";
import PropTypes from "prop-types";

const Resume = props => {
  return <input type="button" className={"button input type"} value={6} />;
};

Resume.propTypes = {};

export default Resume;
