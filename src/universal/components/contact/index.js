import React from "react";
import PropTypes from "prop-types";

const Contact = props => {
  return <input type="button" className={"button input type"} value={3} />;
};

Contact.propTypes = {};

export default Contact;
